/*模型修饰功能 */
var model = {
  facet_code: {
    table_code: {
        fields:[{
            code //通过code判断匹配的扩展模型
        }],
        actions:[{
            code, 
            type  //根据code或type指定扩展
        }]
    }
  }
}
